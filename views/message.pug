div(id="message_"+message.id).message
    div.row
        div.col.s6.m3.message-date #{message.createdAt}
        div.col.s6.m3.message-sender #{message.user.firstname} #{message.user.lastname}
    div.row.message-content #{message.content}

